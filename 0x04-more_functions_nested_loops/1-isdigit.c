#include "main.h"

/**
 * _isdigit - checks if an argument is a digit or not
 * @c: argument to check
 * Return: 1 if digit and 0 if otherwise
 */

int _isdigit(int c)
{

if(c >= '0' && c <= '9' )
{
return (1);
}
else
{
return (0);
}
        
}
